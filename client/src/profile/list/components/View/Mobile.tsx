import { TableListProps } from '../../../../core/types';
import { Profile } from '../../../../profile/list/domain';

export const Mobile = (props: TableListProps<Profile>) => {
  return <div>Mobile</div>;
};
